DATABASE_URL="postgresql://postgres:postgres@localhost:5432/clothing_shop?schema=public"
DIRECT_URL="postgresql://postgres:postgres@localhost:5432/clothing_shop?schema=public"
REDIS_URL="redis://localhost:6379"
JWT_SECRET="change-me-cms-access-secret"
JWT_REFRESH_SECRET="change-me-cms-refresh-secret"
JWT_ACCESS_EXPIRES_IN="15m"
JWT_REFRESH_EXPIRES_IN="7d"
PORT=3002
CMS_ORIGIN="http://localhost:5173"
CLOUDINARY_CLOUD_NAME=""
CLOUDINARY_API_KEY=""
CLOUDINARY_API_SECRET=""
# Token API GHN (đăng ký ở https://khachhang.ghn.vn) — dùng để đồng bộ Tỉnh/Quận/Phường
# (LocationsService.syncFromGhn()) và sau này tạo đơn vận chuyển. Sandbox dùng đúng base
# URL mặc định bên dưới; production đổi sang https://online-gateway.ghn.vn/shiip/public-api.
GHN_API_TOKEN=""
GHN_API_BASE_URL="https://dev-online-gateway.ghn.vn/shiip/public-api"
SENTRY_DSN=""

# Chỉ backend-cms chạy migration (prisma migrate dev). backend-user chỉ chạy `prisma generate`
# vì cả hai cùng trỏ chung 1 database — xem ghi chú trong prisma/schema.prisma và README.
